---
date: 2026-06-25
tags: [ruoyi-dkd, 前端, Vue3, ElementPlus]
---

# 点位管理模块前端改造

## 一、写在前面

1. 为什么学这个：若依代码生成器生成的代码是通用模板，实际业务需要根据原型定制页面布局和功能
2. 学完这篇你能掌握：若依生成代码的改造思路、Element Plus 组件实战（表格、对话框、描述列表）、前后端字段对接

## 二、前置准备

- 后端接口已就绪（区域、合作商、点位的 CRUD 接口）
- 若依代码生成器已生成基础代码并导入项目
- 参考需求文档中的页面原型

## 三、正文分步讲解

### 3.1 区域管理改造

#### 基础表格改造

若依生成的表格列需要根据原型调整：

```html
<el-table v-loading="loading" :data="regionList" @selection-change="handleSelectionChange">
  <el-table-column type="selection" width="55" align="center" />
  <!-- type="index" 自动生成递增序号 -->
  <el-table-column label="序号" type="index" width="50" align="center" prop="id" />
  <el-table-column label="区域名称" align="center" prop="regionName" />
  <!-- 点位数：后端通过关联查询返回 nodeCount 字段 -->
  <el-table-column label="点位数" align="center" prop="nodeCount" />
  <el-table-column label="备注说明" align="center" prop="remark" />
  <el-table-column label="操作" align="center" class-name="small-padding fixed-width">
    <template #default="scope">
      <el-button link type="primary" @click="handleUpdate(scope.row)" v-hasPermi="['manage:region:edit']">修改</el-button>
      <el-button link type="primary" @click="handleDelete(scope.row)" v-hasPermi="['manage:region:remove']">删除</el-button>
    </template>
  </el-table-column>
</el-table>
```

**关键点**：
- `type="index"` 让序号列自动递增，不需要后端返回
- `prop="nodeCount"` 对应后端 RegionVo 的 nodeCount 字段（关联查询统计点位数）
- 操作按钮去掉了 icon，保持简洁

### 3.2 合作商管理改造

#### 搜索区域

```html
<el-form :model="queryParams" ref="queryRef" :inline="true" v-show="showSearch" label-width="90px">
  <el-form-item label="合作商名称" prop="partnerName">
    <el-input v-model="queryParams.partnerName" placeholder="请输入合作商名称"
      clearable @keyup.enter="handleQuery"/>
  </el-form-item>
</el-form>
```

#### 表格列定制

```html
<el-table-column label="分成比例" align="center" prop="profitRatio">
  <!-- 用插槽在数值后面加 % 号 -->
  <template #default="scope">{{scope.row.profitRatio}}%</template>
</el-table-column>
```

#### 表单字段控制

```html
<!-- 编辑时显示创建时间，新增时不显示 -->
<el-form-item label="创建时间" v-if="form.id!=null">
  {{form.createTime}}
</el-form-item>

<!-- 新增时显示账号密码，编辑时不显示 -->
<el-form-item label="账号" prop="account" v-if="form.id==null">
  <el-input v-model="form.account" placeholder="请输入账号" />
</el-form-item>
<el-form-item label="密码" prop="password" v-if="form.id==null">
  <el-input v-model="form.password" type="password" placeholder="请输入密码" />
</el-form-item>
```

**关键点**：
- `v-if="form.id!=null"` 编辑模式（有 id）才显示
- `v-if="form.id==null"` 新增模式（无 id）才显示
- 密码框加 `type="password"` 隐藏输入

#### 查看详情

用 `el-descriptions` 组件展示详情，比表格更整洁：

```html
<el-dialog title="合作商详情" v-model="partnerInfoOpen" width="500px" append-to-body>
  <el-descriptions :column="2" border>
    <el-descriptions-item label="合作商名称">{{ form.partnerName }}</el-descriptions-item>
    <el-descriptions-item label="联系人">{{ form.contactPerson }}</el-descriptions-item>
    <el-descriptions-item label="联系电话">{{ form.contactPhone }}</el-descriptions-item>
    <el-descriptions-item label="分成比例">{{ form.profitRatio }}%</el-descriptions-item>
  </el-descriptions>
</el-dialog>
```

脚本部分：

```javascript
const partnerInfoOpen = ref(false);

function getPartnerInfo(row) {
  reset();
  const _id = row.id;
  getPartner(_id).then(response => {
    form.value = response.data;
    partnerInfoOpen.value = true;
  });
}
```

### 3.3 点位管理改造

#### 下拉选择替代输入框

点位表单中，区域和合作商用下拉选择而不是手动输入：

```html
<el-form-item label="所在区域" prop="regionId">
  <el-select v-model="form.regionId" placeholder="请选择区域" clearable>
    <el-option v-for="item in regionList" :key="item.id" :label="item.regionName" :value="item.id" />
  </el-select>
</el-form-item>
```

页面加载时查询所有区域和合作商：

```javascript
import { listPartner } from "@/api/manage/partner";
import { listRegion } from "@/api/manage/region";
import { loadAllParams } from "@/api/page";

const partnerList = ref([]);
function getPartnerList() {
  listPartner(loadAllParams).then(response => {
    partnerList.value = response.rows;
  });
}

const regionList = ref([]);
function getRegionList() {
  listRegion(loadAllParams).then(response => {
    regionList.value = response.rows;
  });
}

getPartnerList();
getRegionList();
```

#### 字典组件

商圈类型用字典组件显示：

```html
<el-table-column label="商圈类型" align="center" prop="businessType">
  <template #default="scope">
    <dict-tag :options="business_type" :value="scope.row.businessType" />
  </template>
</el-table-column>
```

## 四、方案对比

### 点位数/设备数统计方案

| 方案 | 优点 | 缺点 | 适用场景 |
|---|---|---|---|
| 同步存储（冗余字段） | 查询效率高 | 需要同步维护，数据可能不一致 | 数据量大、查询频繁 |
| 关联查询 | 实时准确，无需维护 | SQL 复杂，数据量大时性能低 | 数据量小、实时性要求高 |

**选择**：区域/合作商/点位数据量不大，采用关联查询方案。

## 五、踩坑避坑指南

### 逻辑坑

**坑 1：v-if 和 v-show 的选择**
- 表单字段用 `v-if` 控制显示/隐藏，因为不需要频繁切换
- `v-if` 直接销毁/重建 DOM，`v-show` 只是 CSS 隐藏
- 表单中编辑/新增字段切换用 `v-if` 更合适

**坑 2：密码回显安全问题**
- 编辑时不显示密码字段（`v-if="form.id==null"`）
- 防止密码明文回显到前端

## 六、效果验证

- 区域管理：表格显示序号、区域名称、点位数、备注说明、操作
- 合作商管理：表格显示序号、合作商名称、点位数、账号、分成比例（带%）、联系人、联系电话、操作（查看详情/修改/删除）
- 点位管理：区域和合作商用下拉选择，商圈类型用字典标签显示

## 七、全文总结

- 若依生成的代码是骨架，需要根据业务原型定制
- `v-if="form.id!=null"` 控制编辑/新增模式的字段显示
- `el-descriptions` 适合展示详情信息
- 关联查询适合数据量小的场景，同步存储适合高频查询

## 八、关联笔记

- [[projects/ruoyi-dkd/项目概述]]
- [[projects/ruoyi-dkd/前端/前端概述]]

## 九、代码变更

- 修改：`src/views/manage/region/index.vue` — 表格序号列改造、添加点位数列
- 修改：`src/views/manage/partner/index.vue` — 搜索/表格/表单全面改造、添加查看详情功能
