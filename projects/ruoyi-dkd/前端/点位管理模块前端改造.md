---
date: 2026-06-25
tags: [ruoyi-dkd, 前端, Vue3, ElementPlus]
---

# 点位管理模块前端改造

## 一、写在前面

1. 为什么学这个：若依代码生成器生成的代码是通用模板，实际业务需要根据原型定制页面布局和功能
2. 学完这篇你能掌握：若依生成代码的改造思路、Element Plus 组件实战（表格、对话框、描述列表）、前后端字段对接

## 二、前置准备

- 后端接口已就绪（区域、合作商、点位的 CRUD 接口）
- 若依代码生成器已生成基础代码并导入项目
- 参考需求文档中的页面原型

## 三、正文分步讲解

### 3.1 区域管理改造

#### 基础表格改造

若依生成的表格列需要根据原型调整：

```html
<el-table v-loading="loading" :data="regionList" @selection-change="handleSelectionChange">
  <el-table-column type="selection" width="55" align="center" />
  <!-- type="index" 自动生成递增序号 -->
  <el-table-column label="序号" type="index" width="50" align="center" prop="id" />
  <el-table-column label="区域名称" align="center" prop="regionName" />
  <!-- 点位数：后端通过关联查询返回 nodeCount 字段 -->
  <el-table-column label="点位数" align="center" prop="nodeCount" />
  <el-table-column label="备注说明" align="center" prop="remark" />
  <el-table-column label="操作" align="center" class-name="small-padding fixed-width">
    <template #default="scope">
      <el-button link type="primary" @click="handleUpdate(scope.row)" v-hasPermi="['manage:region:edit']">修改</el-button>
      <el-button link type="primary" @click="handleDelete(scope.row)" v-hasPermi="['manage:region:remove']">删除</el-button>
    </template>
  </el-table-column>
</el-table>
```

**关键点**：
- `type="index"` 让序号列自动递增，不需要后端返回
- `prop="nodeCount"` 对应后端 RegionVo 的 nodeCount 字段（关联查询统计点位数）
- 操作按钮去掉了 icon，保持简洁

### 3.2 合作商管理改造

#### 搜索区域

```html
<el-form :model="queryParams" ref="queryRef" :inline="true" v-show="showSearch" label-width="90px">
  <el-form-item label="合作商名称" prop="partnerName">
    <el-input v-model="queryParams.partnerName" placeholder="请输入合作商名称"
      clearable @keyup.enter="handleQuery"/>
  </el-form-item>
</el-form>
```

#### 表格列定制

```html
<el-table-column label="分成比例" align="center" prop="profitRatio">
  <!-- 用插槽在数值后面加 % 号 -->
  <template #default="scope">{{scope.row.profitRatio}}%</template>
</el-table-column>
```

#### 表单字段控制

```html
<!-- 编辑时显示创建时间，新增时不显示 -->
<el-form-item label="创建时间" v-if="form.id!=null">
  {{form.createTime}}
</el-form-item>

<!-- 新增时显示账号密码，编辑时不显示 -->
<el-form-item label="账号" prop="account" v-if="form.id==null">
  <el-input v-model="form.account" placeholder="请输入账号" />
</el-form-item>
<el-form-item label="密码" prop="password" v-if="form.id==null">
  <el-input v-model="form.password" type="password" placeholder="请输入密码" />
</el-form-item>
```

**关键点**：
- `v-if="form.id!=null"` 编辑模式（有 id）才显示
- `v-if="form.id==null"` 新增模式（无 id）才显示
- 密码框加 `type="password"` 隐藏输入

#### 查看详情

用 `el-descriptions` 组件展示详情，比表格更整洁：

```html
<el-dialog title="合作商详情" v-model="partnerInfoOpen" width="500px" append-to-body>
  <el-descriptions :column="2" border>
    <el-descriptions-item label="合作商名称">{{ form.partnerName }}</el-descriptions-item>
    <el-descriptions-item label="联系人">{{ form.contactPerson }}</el-descriptions-item>
    <el-descriptions-item label="联系电话">{{ form.contactPhone }}</el-descriptions-item>
    <el-descriptions-item label="分成比例">{{ form.profitRatio }}%</el-descriptions-item>
  </el-descriptions>
</el-dialog>
```

脚本部分：

```javascript
const partnerInfoOpen = ref(false);

function getPartnerInfo(row) {
  reset();
  const _id = row.id;
  getPartner(_id).then(response => {
    form.value = response.data;
    partnerInfoOpen.value = true;
  });
}
```

### 3.3 区域管理 — 查看详情功能

区域列表操作列新增"查看详情"按钮，点击弹出对话框显示区域名称和该区域下的点位列表。

#### 按钮与对话框

```html
<el-button link type="primary" @click="getRegionInfo(scope.row)" v-hasPermi="['manage:node:list']">查看详情</el-button>

<el-dialog title="区域详情" v-model="regionInfoOpen" width="500px" append-to-body>
  <el-form-item label="区域名称" prop="regionName">
    <el-input v-model="form.regionName" disabled />
  </el-form-item>
  <label>包含点位：</label>
  <el-table :data="nodeList">
    <el-table-column label="序号" type="index" width="50" align="center" />
    <el-table-column label="点位名称" align="center" prop="nodeName" />
    <el-table-column label="设备数量" align="center" prop="vmCount" />
  </el-table>
</el-dialog>
```

#### 脚本实现

```javascript
import { listNode } from "@/api/manage/node";
import { loadAllParams } from "@/api/page";

const nodeList = ref([]);
const regionInfoOpen = ref(false);

function getRegionInfo(row) {
  reset();
  getRegion(row.id).then(response => {
    form.value = response.data;
  });
  // 复用 loadAllParams，加上 regionId 筛选条件
  loadAllParams.regionId = row.id;
  listNode(loadAllParams).then(response => {
    nodeList.value = response.rows;
  });
  regionInfoOpen.value = true;
}
```

**关键点**：
- `loadAllParams` 是从 `@/api/page` 抽取的公共查询参数（pageNum=1, pageSize=10000），用于加载全量数据
- 动态添加 `regionId` 属性来筛选特定区域的点位
- 权限控制用 `manage:node:list`，因为查看详情涉及点位列表查询

### 3.4 点位管理改造

#### 搜索区域改造

将区域ID输入框改为下拉选择，去掉合作商ID搜索条件：

```html
<el-form-item label="区域搜索" prop="regionId">
  <el-select v-model="queryParams.regionId" placeholder="请选择区域" clearable>
    <el-option v-for="item in regionList" :key="item.id" :label="item.regionName" :value="item.id" />
  </el-select>
</el-form-item>
```

#### 嵌套对象属性展示

后端返回的点位数据包含嵌套的区域和合作商对象，用点号语法访问：

```html
<!-- prop="region.regionName" 访问嵌套对象的属性 -->
<el-table-column label="所在区域" align="center" prop="region.regionName" />
<el-table-column label="合作商" align="center" prop="partner.partnerName" />
```

**注意**：这要求后端接口返回的数据结构是嵌套对象形式：
```json
{
  "nodeName": "XX大厦",
  "region": { "regionName": "华东区" },
  "partner": { "partnerName": "XX公司" }
}
```

#### 下拉选择替代输入框

点位表单中，区域和合作商用下拉选择而不是手动输入：

点位表单中，区域和合作商用下拉选择而不是手动输入：

```html
<el-form-item label="所在区域" prop="regionId">
  <el-select v-model="form.regionId" placeholder="请选择区域" clearable>
    <el-option v-for="item in regionList" :key="item.id" :label="item.regionName" :value="item.id" />
  </el-select>
</el-form-item>
```

页面加载时查询所有区域和合作商：

```javascript
import { listPartner } from "@/api/manage/partner";
import { listRegion } from "@/api/manage/region";
import { loadAllParams } from "@/api/page";

const partnerList = ref([]);
function getPartnerList() {
  listPartner(loadAllParams).then(response => {
    partnerList.value = response.rows;
  });
}

const regionList = ref([]);
function getRegionList() {
  listRegion(loadAllParams).then(response => {
    regionList.value = response.rows;
  });
}

getPartnerList();
getRegionList();
```

#### 字典组件

商圈类型用字典组件显示：

```html
<el-table-column label="商圈类型" align="center" prop="businessType">
  <template #default="scope">
    <dict-tag :options="business_type" :value="scope.row.businessType" />
  </template>
</el-table-column>
```

## 四、方案对比

### 点位数/设备数统计方案

| 方案 | 优点 | 缺点 | 适用场景 |
|---|---|---|---|
| 同步存储（冗余字段） | 查询效率高 | 需要同步维护，数据可能不一致 | 数据量大、查询频繁 |
| 关联查询 | 实时准确，无需维护 | SQL 复杂，数据量大时性能低 | 数据量小、实时性要求高 |

**选择**：区域/合作商/点位数据量不大，采用关联查询方案。

## 五、踩坑避坑指南

### 逻辑坑

**坑 1：v-if 和 v-show 的选择**
- 表单字段用 `v-if` 控制显示/隐藏，因为不需要频繁切换
- `v-if` 直接销毁/重建 DOM，`v-show` 只是 CSS 隐藏
- 表单中编辑/新增字段切换用 `v-if` 更合适

**坑 2：密码回显安全问题**
- 编辑时不显示密码字段（`v-if="form.id==null"`）
- 防止密码明文回显到前端

## 六、效果验证

- 区域管理：表格显示序号、区域名称、点位数、备注说明、操作
- 合作商管理：表格显示序号、合作商名称、点位数、账号、分成比例（带%）、联系人、联系电话、操作（查看详情/修改/删除）
- 点位管理：区域和合作商用下拉选择，商圈类型用字典标签显示

## 七、全文总结

- 若依生成的代码是骨架，需要根据业务原型定制
- `v-if="form.id!=null"` 控制编辑/新增模式的字段显示
- `el-descriptions` 适合展示详情信息
- 关联查询适合数据量小的场景，同步存储适合高频查询

## 八、关联笔记

- [[projects/ruoyi-dkd/项目概述]]
- [[projects/ruoyi-dkd/前端/前端概述]]

### 3.5 人员管理改造

#### 搜索区域精简

若依生成的搜索表单字段太多，根据实际需求精简为只保留人员名称：

```html
<el-form-item label="人员名称" prop="userName">
  <el-input v-model="queryParams.userName" placeholder="请输入人员名称" clearable @keyup.enter="handleQuery" />
</el-form-item>
```

对应的 queryParams 也要同步精简，去掉不需要的字段。

#### 角色和区域下拉选择

对话框中角色和区域都改为下拉选择：

```html
<el-form-item label="角色" prop="roleId">
  <el-select v-model="form.roleId" placeholder="请选择角色">
    <el-option v-for="item in roleList" :key="item.roleId" :label="item.roleName" :value="item.roleId" />
  </el-select>
</el-form-item>

<el-form-item label="负责区域" prop="regionId">
  <el-select v-model="form.regionId" placeholder="请选择所属区域">
    <el-option v-for="item in regionList" :key="item.id" :label="item.regionName" :value="item.id" />
  </el-select>
</el-form-item>
```

脚本加载角色和区域列表：

```javascript
import { listRegion } from "@/api/manage/region";
import { listRole } from "@/api/manage/role";
import { loadAllParams } from "@/api/page";

const roleList = ref([]);
function getRoleList() {
  listRole(loadAllParams).then(response => {
    roleList.value = response.rows;
  });
}

const regionList = ref([]);
function getRegionList() {
  listRegion(loadAllParams).then(response => {
    regionList.value = response.rows;
  });
}

getRoleList();
getRegionList();
```

#### 创建时间字段控制

编辑时显示创建时间，新增时不显示：

```html
<el-form-item label="创建时间" prop="createTime" v-if="form.id!=null">
  {{form.createTime}}
</el-form-item>
```

#### 员工头像上传

使用若依全局组件 `image-upload` 实现头像上传：

```html
<el-form-item label="员工头像" prop="image">
  <image-upload v-model="form.image" />
</el-form-item>
```

#### 是否启用（字典+单选框）

```html
<el-form-item label="是否启用" prop="status">
  <el-radio-group v-model="form.status">
    <el-radio v-for="dict in emp_status" :key="dict.value" :label="parseInt(dict.value)">{{ dict.label }}</el-radio>
  </el-radio-group>
</el-form-item>
```

**关键点**：
- `emp_status` 字典通过 `proxy.useDict('emp_status')` 获取
- `:label="parseInt(dict.value)"` 将字典值转为数字，与后端状态值类型一致

### 3.6 设备类型管理改造

#### 搜索精简

只保留型号名称搜索，去掉型号编码：

```html
<el-form-item label="型号名称" prop="name">
  <el-input v-model="queryParams.name" placeholder="请输入型号名称" clearable @keyup.enter="handleQuery" />
</el-form-item>
```

#### el-input-number 数字输入框

货道行/列和容量用数字输入框，带单位后缀：

```html
<el-form-item label="货道数" prop="vmRow">
  <el-input-number v-model="form.vmRow" placeholder="请输入" :min="1" :max="10"/>行&nbsp;&nbsp;
  <el-input-number v-model="form.vmCol" placeholder="请输入" :min="1" :max="10"/>列
</el-form-item>

<el-form-item label="货道容量" prop="channelMaxCapacity">
  <el-input-number v-model="form.channelMaxCapacity" placeholder="请输入" :min="1" :max="10"/>个
</el-form-item>
```

**关键点**：
- `:min="1"` 设置最小值，防止输入 0 或负数
- `:max="10"` 设置最大值，限制合理范围
- 单位文字（行、列、个）直接写在组件后面，用 `&nbsp;` 空格分隔

### 3.7 设备管理改造

#### 表格中 v-for 匹配显示名称

后端只返回 ID（如 vmTypeId、partnerId），需要在前端通过 v-for 循环匹配显示名称：

```html
<el-table-column label="设备型号" align="center" prop="vmTypeId">
  <template #default="scope">
    <div v-for="item in vmTypeList" :key="item.id">
      <span v-if="item.id==scope.row.vmTypeId">{{ item.name }}</span>
    </div>
  </template>
</el-table-column>
```

**适用场景**：后端没有做关联查询，只返回了 ID 字段，前端需要自行匹配显示名称。

#### 对话框条件渲染（新增 vs 编辑）

根据 `form.innerCode` 是否为 null 区分新增和编辑模式，显示不同字段：

```html
<!-- 新增时：系统自动生成编号 -->
<el-form-item label="设备编号">
  <span>{{ form.innerCode == null ? '系统自动生成' : form.innerCode }}</span>
</el-form-item>

<!-- 编辑时才显示的字段 -->
<el-form-item label="供货时间" v-if="form.innerCode != null">...</el-form-item>
<el-form-item label="设备类型" v-if="form.innerCode != null">...</el-form-item>
<el-form-item label="设备容量" v-if="form.innerCode != null">...</el-form-item>
<el-form-item label="合作商" v-if="form.innerCode != null">...</el-form-item>
<el-form-item label="所属区域" v-if="form.innerCode != null">...</el-form-item>
<el-form-item label="设备地址" v-if="form.innerCode != null">...</el-form-item>

<!-- 新增时才显示的字段 -->
<el-form-item label="选择型号" prop="vmTypeId" v-if="form.innerCode == null">
  <el-select v-model="form.vmTypeId" placeholder="请选择设备型号" style="width: 100%">
    <el-option v-for="item in vmTypeList" :key="item.id" :label="item.name" :value="item.id" />
  </el-select>
</el-form-item>
```

**关键点**：
- 新增模式（innerCode == null）：显示型号选择下拉
- 编辑模式（innerCode != null）：显示只读的设备信息（类型、容量、合作商、区域、地址）
- 点位选择在两种模式下都显示（可修改）

#### 加载多个下拉列表

设备管理页面需要同时加载 4 个下拉列表：

```javascript
import { listVmType } from "@/api/manage/vmType";
import { listPartner } from "@/api/manage/partner";
import { listNode } from "@/api/manage/node";
import { listRegion } from "@/api/manage/region";
import { loadAllParams } from "@/api/page";

const vmTypeList = ref([]);
const partnerList = ref([]);
const nodeList = ref([]);
const regionList = ref([]);

// 加载函数...

getRegionList();
getPartnerList();
getNodeList();
getVmTypeList();
```

### 3.8 设备状态页面

设备状态是独立于设备管理的页面（vmStatus/index.vue），专注于查看设备运行状态，不需要增删改功能。

#### 运营状态 vs 设备状态

两个不同的概念，用不同方式展示：

```html
<!-- 运营状态：使用字典标签，如 未投放/运营/撤机 -->
<el-table-column label="运营状态" align="center" prop="vmStatus">
  <template #default="scope">
    <dict-tag :options="vm_status" :value="scope.row.vmStatus"/>
  </template>
</el-table-column>

<!-- 设备状态：解析 JSON 字符串，判断设备是否正常运行 -->
<el-table-column label="设备状态" align="center" prop="vmStatus">
  <template #default="scope">
    {{ scope.row.runningStatus != null ? JSON.parse(scope.row.runningStatus).status == true ? '正常' : '异常' : '异常' }}
  </template>
</el-table-column>
```

**关键点**：
- `runningStatus` 是 JSON 字符串，存储设备硬件状态（如 `{"status": true}`）
- 用 `JSON.parse()` 解析后取 `status` 字段
- 嵌套三元运算符：先判空，再判 status 值

### 3.9 点位查看详情（点位下的设备列表）

在 node/index.vue 中，点击"查看详情"弹出该点位下的设备列表：

```javascript
import { listVm } from "@/api/manage/vm";

const nodeOpen = ref(false);
const vmList = ref([]);

function getNodeInfo(row) {
  loadAllParams.nodeId = row.id;
  listVm(loadAllParams).then(response => {
    vmList.value = response.rows;
    nodeOpen.value = true;
  });
}
```

对话框中显示设备列表，包含设备编号、设备状态（dict-tag）、最后一次供货时间（parseTime 格式化）。

### 3.10 策略管理改造

#### el-input-number 整数输入

折扣比例用数字输入框，限制 1-100 的整数：

```html
<el-form-item label="策略方案" prop="discount">
  <el-input-number :min="1" :max="100" :precision="0" v-model="form.discount" placeholder="请输入策略" />
</el-form-item>
```

**关键点**：`:precision="0"` 表示不允许小数，只输入整数。

#### 策略查看详情

点击"查看详情"显示该策略下的设备列表：

```javascript
function getPolicyInfo(row) {
  form.value = row;
  loadAllParams.policyId = row.policyId;
  listVm(loadAllParams).then(response => {
    vmList.value = response.rows;
    policyOpen.value = true;
  });
}
```

### 3.11 设备策略分配

在 vm/index.vue 中，点击"策略"按钮弹出策略选择对话框，为设备分配策略：

```javascript
const policyList = ref([]);
const policyOpen = ref(false);

function handleUpdatePolicy(row) {
  form.value.id = row.id;
  form.value.policyId = row.policyId;
  listPolicy(loadAllParams).then(response => {
    policyList.value = response.rows;
    policyOpen.value = true;
  });
}
```

对话框中用 el-select 下拉选择策略：

```html
<el-form-item label="策略" prop="policyId">
  <el-select v-model="form.policyId" placeholder="请选择策略">
    <el-option v-for="item in policyList" :key="item.policyId" :label="item.policyName" :value="item.policyId" />
  </el-select>
</el-form-item>
```

**关键点**：
- 取消按钮需要同时关闭两个对话框（`open` 和 `policyOpen`）
- `handleUpdatePolicy` 直接用 `form.value.id = row.id` 赋值，不调用 reset()，保留设备信息

## 九、代码变更

- 修改：`src/views/manage/region/index.vue` — 表格序号列改造、添加点位数列、新增查看详情功能（对话框+点位表格）
- 修改：`src/views/manage/partner/index.vue` — 搜索/表格/表单全面改造、添加查看详情功能、操作按钮顺序调整（重置密码→查看详情→修改→删除）
- 修改：`src/views/manage/node/index.vue` — 搜索区域改为下拉选择、表格列改为嵌套对象属性（region.regionName、partner.partnerName）、表单区域和合作商改为下拉选择、地址改为 textarea、新增查看详情（设备列表）
- 修改：`src/views/manage/emp/index.vue` — 搜索精简为人员名称、表格序号列改造、角色和区域改为下拉选择、新增创建时间字段、去掉操作按钮图标
- 修改：`src/views/manage/vmType/index.vue` — 搜索精简为型号名称、去掉主键列、货道数和容量改为 el-input-number、去掉操作按钮图标
- 修改：`src/views/manage/vm/index.vue` — 搜索精简为设备编号、表格用 v-for 匹配显示型号和合作商名称、对话框新增/编辑模式条件渲染、选择型号和点位改为下拉选择、新增策略分配功能
- 修改：`src/views/manage/vmStatus/index.vue` — 设备状态页面改造（运营状态+设备状态双列展示、查看详情按钮）
- 修改：`src/views/manage/policy/index.vue` — 表格序号列改造、策略方案改为 el-input-number、新增查看详情功能（设备列表）
- 修改：`src/api/manage/partner.js` — 新增 resetPartnerPwd 接口
